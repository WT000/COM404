# Asking the user what happens when the petal falls
print("What happens when the last petal falls?")

# Variable
petal_consequence = input()

# Message with variable
print("My dear Bella when the last petal falls", petal_consequence)